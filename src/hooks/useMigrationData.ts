// Re-export the hook from the provider for backward compatibility
// This file now acts as a simple re-export to maintain existing imports
export { useMigrationData, useFieldValues } from '@/providers/MigrationDataProvider';